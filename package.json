{
  "name": "c05-bank-account-system-cli",
  "version": "0.0.1",
  "description": "A CLI-based bank system in TypeScript that manages multiple accounts and supports async operations.",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "author": "jsalguero.dev",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.3.5",
    "nodemon": "^3.1.14",
    "rimraf": "^6.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
